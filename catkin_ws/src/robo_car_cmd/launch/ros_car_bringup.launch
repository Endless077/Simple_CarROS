<launch>
  <!-- Find the YAML parameter file -->
  <param from="$(find ros_car_cmd)/config/car_params.yaml" command="load"/>

  <!-- Startup the teleop node -->
  <node pkg="ros_car_teleop" type="teleop_node.py" name="teleop_node" output="screen"/>
</launch>
